'use client';

import Link from 'next/link';
import { ArrowRight, BadgePercent, BarChart3, FileBox, Gamepad2, LockKeyhole, ShieldCheck, Sparkles, Wallet, Zap } from 'lucide-react';
import { motion } from 'framer-motion';
import { useEffect, useState } from 'react';
import { Container } from '@/components/ui/Container';
import { Button } from '@/components/ui/Button';
import { SectionTitle } from '@/components/ui/SectionTitle';
import { ProductCard } from '@/components/shop/ProductCard';
import type { Product } from '@/types/shop';

export default function HomePage() {
  const [products, setProducts] = useState<Product[]>([]);
  const [stats, setStats] = useState({ products: 0, sold: 0, users: 0 });
  useEffect(() => { fetch('/api/products?featured=true&limit=6').then((r) => r.json()).then((j) => j.ok && setProducts(j.data.items)).catch(() => undefined); fetch('/api/products/stats').then((r) => r.json()).then((j) => j.ok && setStats(j.data)).catch(() => undefined); }, []);
  return <div>
    <section className="relative overflow-hidden"><Container className="grid min-h-[680px] items-center gap-12 py-20 lg:grid-cols-[1.1fr_.9fr]"><div className="relative z-10"><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-cyan-400/15 bg-cyan-400/[.06] px-3 py-1.5 text-[10px] font-black uppercase tracking-[.2em] text-cyan-200"><span className="h-1.5 w-1.5 rounded-full bg-green-300 shadow-[0_0_14px_rgba(67,230,161,.9)]" />System online • 24/7 storefront</div><h1 className="max-w-3xl text-5xl font-black leading-[.98] tracking-[-.045em] sm:text-6xl lg:text-7xl">Mua nhanh.<br /><span className="lv-gradient-text">Giao tự động.</span><br />Sống chất riêng.</h1><p className="mt-7 max-w-2xl text-base leading-8 text-white/50">Lù A Vang là hệ thống thương mại điện tử dành cho ACC + FILE DIGITAL, kết hợp thanh toán xác thực server-side, ví người dùng và giao hàng tự động.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/shop"><Button className="lv-shine">Khám phá shop <ArrowRight className="h-4 w-4" /></Button></Link><Link href="/deposit"><Button variant="secondary"><Wallet className="h-4 w-4" />Nạp tiền</Button></Link></div><div className="mt-10 grid max-w-xl grid-cols-3 gap-3">{[['Sản phẩm', stats.products], ['Đã bán', stats.sold], ['Tài khoản', stats.users]].map(([label, value]) => <div key={label} className="lv-glass rounded-2xl p-4"><div className="text-xl font-black">{Number(value).toLocaleString('vi-VN')}</div><div className="mt-1 text-[10px] font-bold uppercase tracking-[.17em] text-white/35">{label}</div></div>)}</div></div><div className="relative hidden min-h-[500px] lg:block"><div className="absolute left-[18%] top-[8%] h-[360px] w-[360px] rounded-full border border-purple-400/15 bg-purple-500/[.06] blur-[1px] shadow-[0_0_130px_rgba(155,92,255,.25)]" /><div className="absolute right-[4%] top-[22%] h-[240px] w-[240px] rounded-full border border-cyan-400/15 bg-cyan-400/[.04] shadow-[0_0_110px_rgba(53,201,255,.18)]" /><motion.div initial={{ opacity: 0, scale: .92 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: .8 }} className="lv-glass lv-glow lv-float absolute inset-x-4 top-24 rounded-3xl p-5"><div className="mb-5 flex items-center justify-between"><span className="flex items-center gap-2 text-xs font-bold text-white/60"><Sparkles className="h-4 w-4 text-purple-300" /> LIVE CONTROL</span><span className="rounded-full bg-green-400/10 px-2 py-1 text-[10px] font-black text-green-300">SYSTEM READY</span></div><div className="grid grid-cols-2 gap-3"><div className="rounded-2xl bg-black/25 p-5"><div className="text-[10px] text-white/35">WALLET FLOW</div><div className="mt-2 text-2xl font-black">SERVER VERIFIED</div><div className="mt-3 h-1.5 rounded-full bg-white/5"><div className="h-full w-[68%] rounded-full bg-gradient-to-r from-purple-500 to-cyan-400" /></div></div><div className="rounded-2xl bg-black/25 p-5"><div className="text-[10px] text-white/35">DELIVERY</div><div className="mt-2 text-2xl font-black">AUTO</div><div className="mt-3 text-xs text-cyan-200/70">Server verified</div></div></div><div className="mt-3 rounded-2xl border border-pink-400/10 bg-gradient-to-r from-pink-500/[.07] to-orange-400/[.04] p-5"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-pink-500/15"><Zap className="h-5 w-5 text-pink-200" /></div><div><div className="text-sm font-bold">Instant purchase pipeline</div><div className="mt-1 text-xs text-white/35">Price → balance → inventory → delivery</div></div></div></div></motion.div></div></Container></section>

    <Container className="pb-20"><SectionTitle eyebrow="catalog" title="Kho hàng chọn lọc" desc="Phân tách rõ ACC và FILE, tìm nhanh bằng search và lọc theo nhu cầu." /><div className="mb-7 grid grid-cols-2 gap-3 md:grid-cols-4"><Link href="/shop?category=acc" className="lv-glass group rounded-2xl p-5 hover:border-purple-400/25"><Gamepad2 className="h-6 w-6 text-purple-300" /><div className="mt-5 font-black">ACC</div><div className="mt-1 text-xs text-white/35">Tài khoản số</div></Link><Link href="/shop?category=file" className="lv-glass group rounded-2xl p-5 hover:border-cyan-400/25"><FileBox className="h-6 w-6 text-cyan-300" /><div className="mt-5 font-black">FILE</div><div className="mt-1 text-xs text-white/35">Digital downloads</div></Link><div className="lv-glass rounded-2xl p-5"><BadgePercent className="h-6 w-6 text-pink-300" /><div className="mt-5 font-black">Voucher</div><div className="mt-1 text-xs text-white/35">Mã giảm giá</div></div><div className="lv-glass rounded-2xl p-5"><ShieldCheck className="h-6 w-6 text-green-300" /><div className="mt-5 font-black">Bảo vệ</div><div className="mt-1 text-xs text-white/35">Server verified</div></div></div><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map((p) => <ProductCard key={p.id} product={p} />)}</div>{!products.length && <div className="lv-glass rounded-2xl p-12 text-center text-sm text-white/35">Chưa có sản phẩm nổi bật. Admin có thể thêm sản phẩm sau khi cấu hình Firebase.</div>}</Container>

    <Container className="pb-20"><div className="grid gap-5 lg:grid-cols-3"><div className="lv-glass rounded-2xl p-6"><div className="text-xs font-black uppercase tracking-[.2em] text-pink-300/70">Khuyến mãi</div><h3 className="mt-2 text-2xl font-black">Deal đang chạy</h3><div className="mt-5 space-y-3">{products.filter(p=>(p.discount??0)>0).slice(0,3).map(p=><Link key={p.id} href={`/product/${p.slug}`} className="block rounded-xl bg-black/20 p-4 hover:bg-white/[.05]"><div className="flex justify-between gap-3 text-sm font-bold"><span className="truncate">{p.name}</span><span className="text-pink-300">-{p.discount}%</span></div><div className="mt-1 text-xs text-white/30">{p.inventoryCount} còn</div></Link>)}{!products.some(p=>(p.discount??0)>0)&&<p className="text-sm text-white/35">Chưa có deal đang hoạt động.</p>}</div></div><div className="lv-glass rounded-2xl p-6"><div className="text-xs font-black uppercase tracking-[.2em] text-cyan-300/70">Mới & nổi bật</div><h3 className="mt-2 text-2xl font-black">Kho vừa cập nhật</h3><div className="mt-5 space-y-3">{[...products].sort((a,b)=>b.createdAt-a.createdAt).slice(0,3).map(p=><Link key={p.id} href={`/product/${p.slug}`} className="block rounded-xl bg-black/20 p-4 hover:bg-white/[.05]"><div className="text-sm font-bold">{p.name}</div><div className="mt-1 text-xs text-cyan-200/50">{p.category} • {p.soldCount} đã bán</div></Link>)}</div></div><div className="lv-glass rounded-2xl p-6"><div className="text-xs font-black uppercase tracking-[.2em] text-green-300/70">Hướng dẫn mua</div><h3 className="mt-2 text-2xl font-black">4 bước</h3><div className="mt-5 space-y-4 text-sm text-white/45">{['Đăng nhập hoặc đăng ký','Nạp tiền vào ví','Chọn ACC / FILE và checkout','Nhận giao hàng trong đơn'].map((x,i)=><div key={x} className="flex gap-3"><span className="grid h-7 w-7 shrink-0 place-items-center rounded-lg bg-purple-500/10 text-xs font-black text-purple-200">{i+1}</span><span>{x}</span></div>)}</div></div></div></Container>

    <section className="border-y border-white/[.06] bg-black/10"><Container className="py-16"><SectionTitle eyebrow="payment" title="Thanh toán & FAQ" desc="Chỉ giao dịch được xác nhận server-side mới làm thay đổi số dư hoặc tạo giao hàng."/><div className="grid gap-4 md:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-black/20 p-6"><div className="font-black">Phương thức thanh toán</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-white/[.04] p-4"><div className="font-bold text-cyan-200">SePay / Bank</div><div className="mt-1 text-xs text-white/35">Deposit code + webhook settlement</div></div><div className="rounded-xl bg-white/[.04] p-4"><div className="font-bold text-pink-200">NAPPay / Card</div><div className="mt-1 text-xs text-white/35">Adapter chờ API contract chính thức</div></div></div></div><div className="space-y-3"><details className="group rounded-2xl border border-white/10 bg-black/20 p-5"><summary className="cursor-pointer list-none font-bold">Mua ACC/file có tự động giao không?</summary><p className="mt-3 text-sm leading-6 text-white/40">Có, khi checkout được server xác nhận thành công; ACC được lock trong transaction và file được cấp signed URL ngắn hạn.</p></details><details className="group rounded-2xl border border-white/10 bg-black/20 p-5"><summary className="cursor-pointer list-none font-bold">Vì sao nạp tiền có thể ở Pending?</summary><p className="mt-3 text-sm leading-6 text-white/40">Deposit chỉ thành công khi provider/webhook được xác thực và amount khớp. Không có frontend-only success.</p></details></div></div></Container></section>

    <section className="border-b border-white/[.06] bg-black/10"><Container className="grid gap-8 py-16 md:grid-cols-3">{[[LockKeyhole,'Bảo vệ dữ liệu','ACC inventory private; file trả phí chỉ cấp quyền sau thanh toán.'],[BarChart3,'Minh bạch giao dịch','Deposit và order có trạng thái, mã giao dịch và lịch sử.'],[Sparkles,'Trải nghiệm sống động','Neon, gradient, glass và micro-interactions được giữ ở mức hiệu năng hợp lý.']].map(([Icon,title,text]) => <div key={title as string} className="lv-glass rounded-2xl p-6"><Icon className="h-6 w-6 text-cyan-300" /><h3 className="mt-5 font-black">{title as string}</h3><p className="mt-2 text-sm leading-6 text-white/40">{text as string}</p></div>)}</Container></section>
  </div>;
}
